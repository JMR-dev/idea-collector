# fail2ban filter: failed invite-code attempts (HTTP 401 on /api/auth) in Caddy's
# JSON access log. <HOST> binds the real client IP that Caddy logs as client_ip.
[Definition]
failregex = ^.*"client_ip":"<HOST>".*"uri":"/api/auth".*"status":401
datepattern = {EPOCH}
